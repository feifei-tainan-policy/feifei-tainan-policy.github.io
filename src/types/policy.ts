export type Policy = {
  id: string;
  number: string;
  shortTitle: string;
  title: string;
  english: string;
  summary: string;
  points: string[];
  accent: string;
  tint: string;
  videoPath?: string;
  posterPath?: string;
};
